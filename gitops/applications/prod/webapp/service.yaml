---
apiVersion: v1
kind: Service
metadata:
  name: webapp
  labels:
    app: webapp
    environment: prod
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: webapp
